/**
 * Node half of @myflv/dsh-mobile-fix: an empty host-side plugin entry.
 * The browser half lives in src/client/index.ts.
 */

export const inject: string[] = []

/** Node half has no side effects. */
export function apply(): void {}
